default: --tags ~@wip --strict features
all: --strict features
fast: --tags ~@javascript --tags ~@wip --strict features
html_report: --tags ~@wip --strict --format html --out=features_report.html features
